# Phase 7-A2：FastAPI 后端 Render 部署所需最小依赖（backend/main.py）
# Build：pip install -r requirements.txt
# Start：uvicorn backend.main:app --host 0.0.0.0 --port $PORT

fastapi>=0.109.0,<1
uvicorn[standard]>=0.27.0,<1
sqlalchemy>=2.0,<3
psycopg2-binary>=2.9.0,<3
pydantic>=2.0,<3
python-multipart
passlib[bcrypt]>=1.7.4
# passlib 1.7.4 与 bcrypt 4.1+ 不兼容（backend 检测/哈希会报错）
bcrypt>=4.0.1,<4.1
