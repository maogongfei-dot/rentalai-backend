# Selectors（P6 预留）

按平台分文件集中管理列表页 selector（与 DOM 实填同步迭代）：

- **`rightmove_selectors.py`** — Rightmove 列表页（Phase3+ 已使用）
- **`zoopla_selectors.py`** — Zoopla 列表页（**P7 Phase1** 起实填关键 selector）

命名与 DOM 以实际页面为准，需配合 Playwright 在 **小范围、少页数** 下迭代。
