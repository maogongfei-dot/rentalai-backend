# RentalAI 静态前端（web_public）
#
# 当前策略：API 与页面同域，仅使用相对路径（见 assets/api_config.js）。
# 由同一 FastAPI 进程（python run.py / Render Web Service）提供页面与 /api/*，无需配置公网 API 根地址。
#
# 若将来恢复「静态站与 API 分域」部署，需另行改 api_config 与 CORS；本文件不再用于注入 meta。
