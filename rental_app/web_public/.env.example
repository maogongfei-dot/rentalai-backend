# RentalAI 静态前端（web_public）— 构建 / 分域部署时可选
#
# 同源（python run.py 提供页面 + API）：无需配置；assets/api_config.js 使用相对路径。
#
# 分域（例如 Vercel 静态 + Render API）— 在 CI / Vercel Build 环境变量中设置其一：
#   RENTALAI_API_BASE=https://your-api.onrender.com
#   或 VITE_RENTALAI_API_BASE=...（与 scripts/inject-api-base.mjs 一致）
# 然后：cd web_public && npm run build
#
# 可选：RENTALAI_ENV=production → 写入各页 <meta name="rentalai-env">（与 inject-api-base.mjs）
#
# 运行时由页面 meta 与 api_config.js 设置 window.RENTALAI_API_BASE / rentalaiApiUrl()。
