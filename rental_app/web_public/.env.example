# RentalAI 静态前端（web_public）— 构建时注入 API 根地址
#
# 本地：不设置变量；两则 meta 留空 → api_config.js 用相对路径，与 python run.py 同源。
#
# Vercel：Project → Environment Variables（Build）优先设置：
#   VITE_RENTALAI_API_BASE=https://你的后端.onrender.com
# 其次可设 RENTALAI_API_BASE（inject 脚本会写入 rentalai-api-base meta）。
# 然后执行 npm run build（见 package.json），运行 ../scripts/inject-api-base.mjs。
#
# 输出目录：即 web_public 本身，无 dist/。
#
# 优先（与运行时 api_config 一致）
VITE_RENTALAI_API_BASE=

# 备选
RENTALAI_API_BASE=

# 其它别名（inject 同样读取）
# NEXT_PUBLIC_RENTALAI_API_BASE=
# VITE_API_BASE_URL=
