# RentalAI — core install
#   cd rental_app
#   pip install -r requirements.txt
#
# 真实房源抓取（异步 /tasks 多源分析）需要 Chromium：
#   playwright install chromium

# Phase4 Web 产品（FastAPI + 静态 Phase3 UI）
fastapi>=0.109.0,<1
uvicorn[standard]>=0.27.0,<1
pydantic>=2.0,<3

# 合同 PDF 文本提取（Phase B5，文本层；非 OCR）
pypdf>=4.0,<6

# Phase 3 合同 DOCX 段落提取（contract_analysis.contract_document_reader）
python-docx>=1.0.0,<2

# HTTP 客户端（Streamlit 调 API、部分脚本）
requests>=2.31.0

# Zoopla 列表页 requests + HTML 解析（scraper/zoopla_scraper.py）
beautifulsoup4>=4.12.0

# Phase D3：可选 RENTALAI_ZOOPLA_FETCH_MODE=playwright（scraper/zoopla_playwright_scraper.py）；安装后需 playwright install chromium
# 列表页抓取（Rightmove / Zoopla / data/scraper）
playwright>=1.40.0

# 旧版 Streamlit 演示界面（可选；仅在使用 app_web.py 时需要）
streamlit>=1.28.0

python-multipart
